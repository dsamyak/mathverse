// HUD is intentionally minimal — player name, XP, streaks and badges
// are managed by the host application. This component is kept as a
// placeholder so imports don't break; it renders nothing.
export default function HUD() {
  return null
}
